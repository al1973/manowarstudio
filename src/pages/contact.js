import React from 'react';
import { Link } from 'gatsby';
import Layout from '../components/layout';

const Contact = () => (
  <Layout>
    <h1>Contact Me</h1>
    <p>Need to put a contact form here.</p>
    <Link to="/">&larr; back to home</Link>
  </Layout>
);

export default Contact;